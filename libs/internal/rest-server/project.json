{
    "name": "rest-server",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "libs/internal/rest-server/src",
    "projectType": "library",
    "tags": ["npm:private"],
    "// targets": "to see all targets run: nx show project rest-server --web",
    "targets": {
      "test": {
        "executor": "@nx/jest:jest",
        "outputs": [
          "{workspaceRoot}/coverage/{projectRoot}"
        ],
        "options": {
          "jestConfig": "libs/internal/rest-server/jest.config.ts",
          "passWithNoTests": true
        },
        "configurations": {
          "ci": {
            "ci": true,
            "codeCoverage": true
          }
        }
      }
    }
  }
  